import { cutText } from '../../../helpers/cut-text';
import { module, test } from 'qunit';

module('Unit | Helper | cut text');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = cutText(42);
  assert.ok(result);
});
